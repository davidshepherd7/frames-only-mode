(package "frames-only-mode" "0.1" "Use frames instead of emacs windows")

(files "frames-only-mode.el")

;; Tell Cask to add package information to this file (so that we can have a
;; single file package).
(package-file "frames-only-mode.el")

(source melpa-stable)
(source gnu)

(development
 (depends-on "ert-runner")
 (depends-on "f")
 (depends-on "ecukes")
 (depends-on "espuds")

 ;; For testing interactions
 (depends-on "magit")
 )
